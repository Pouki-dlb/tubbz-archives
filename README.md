# The TUBBZ Archive

Static site cataloguing TUBBZ collectible ducks, with a browser-only collection tracker.

All documentation (architecture, `data.js` schema, image naming, the local admin tool) is in
[`CLAUDE.md`](CLAUDE.md).
